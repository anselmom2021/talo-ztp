machine:
  network:
    interfaces:
      - interface: eth0
        addresses:
          - 10.0.0.11/24 # CHANGEME per-node
        routes:
          - network: 0.0.0.0/0
            gateway: 10.0.0.1
        vip:
          ip: 10.0.0.10 # CHANGEME API VIP
  install:
    disk: /dev/sda # OS disk
cluster:
  controlPlane:
    endpoint: https://10.0.0.10:6443 # CHANGEME
